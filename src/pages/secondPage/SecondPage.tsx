import * as React from 'react'
import {Typography} from "@mui/material";
import {memo} from "react";

export const SecondPage = memo(() => {
  return <Typography>SecondPage</Typography>
})
SecondPage.displayName = "SecondPage"
